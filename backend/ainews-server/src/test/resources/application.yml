spring:
  datasource:
    url: jdbc:h2:mem:ainews;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none

ainews:
  ses:
    sender-email: test@example.com
    base-url: http://localhost
