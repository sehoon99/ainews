plugins {
    id 'java'
    id 'org.springframework.boot' version '3.4.2'
    id 'io.spring.dependency-management' version '1.1.7'
    id 'org.flywaydb.flyway' version '11.1.0'
}

group = 'com.example'
version = '0.0.1-SNAPSHOT'
description = 'ainews'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-batch'
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-web-services'

    // Database
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    runtimeOnly 'com.mysql:mysql-connector-j'

    // Flyway
    implementation 'org.flywaydb:flyway-core'
    implementation 'org.flywaydb:flyway-mysql'

    // OpenSearch
    implementation 'org.opensearch.client:opensearch-java:2.13.0'
    implementation 'org.opensearch.client:opensearch-rest-client:2.13.0'
    implementation 'org.apache.httpcomponents.client5:httpclient5:5.3.1'
    implementation 'com.fasterxml.jackson.datatype:jackson-datatype-jsr310'

    // AWS SDK v2 (프로덕션 IAM 서명용)
    implementation platform('software.amazon.awssdk:bom:2.25.0')
    implementation 'software.amazon.awssdk:opensearch'
    implementation 'software.amazon.awssdk:auth'
    implementation 'software.amazon.awssdk:apache-client'

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testImplementation 'org.springframework.batch:spring-batch-test'
    testImplementation 'org.springframework.security:spring-security-test'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named('test') {
    useJUnitPlatform()
}

// Flyway CLI 설정 (환경변수로 DB 접속 정보 주입)
flyway {
    url = "jdbc:mysql://${System.env.DB_HOST ?: 'localhost'}:${System.env.DB_PORT ?: '3306'}/${System.env.DB_NAME ?: 'ainews'}"
    user = System.env.DB_USERNAME ?: 'admin'
    password = System.env.DB_PASSWORD ?: ''
    locations = ['classpath:db/migration']
    baselineOnMigrate = true
    cleanDisabled = true  // 운영 DB 보호: clean 명령 비활성화
}
